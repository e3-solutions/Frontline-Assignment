# Core Pipecat with all necessary extras (soundfile added for background audio mixing)
pipecat-ai[daily,deepgram,cartesia,openai,silero,local-smart-turn-v3,runner,soundfile]==0.0.95

# Pipecat Cloud for production deployment
pipecatcloud>=0.2.7

# Web server and HTTP client
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
aiohttp>=3.10.0
httpx>=0.27.0

# Environment and configuration
python-dotenv>=1.0.1

# Logging
loguru>=0.7.0

# Data validation (usually comes with FastAPI but explicit is better)
pydantic>=2.0.0

# Shared package (install with: pip install -e ../shared)
-e ../shared

# Async file operations for audio recording
aiofiles>=24.1.0

# Development tools (optional, uncomment if needed)
# pre-commit>=4.2.0
# ruff>=0.12.1

# Twilio client for SMS operations
twilio>=9.0.0
